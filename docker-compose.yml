services:
  dvl:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 'dvl'
    working_dir: '/root/opt/resource'
    environment:
      - TOKEN=${API_TOKEN}
    volumes:
      - .:/root/opt